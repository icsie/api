# PostgreSQL connection string used by the FastAPI application
# Replace the password with your local PostgreSQL user's password.
DATABASE_URL=postgresql://dev_user:your_password@localhost:5432/fastapi_dev
